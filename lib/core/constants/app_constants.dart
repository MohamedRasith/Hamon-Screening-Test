import 'package:flutter/material.dart';

class AppConstants {
  static const baseUrl = "https://nibrahim.pythonanywhere.com/";
  static const token = "7aD25";
  static const tokenQuery = "?api_key=$token";
  static const defaultPadding = 12.0;
  static const elevation = 3.0;
  static const cornerRadius = 12.0;

  static var borderRadius = BorderRadius.circular(cornerRadius);
}
